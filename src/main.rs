//use bytes::Bytes;

mod common;

fn main() {
    //let mut rbuf = common::recv_buffer::RecvBuffer::with_capacity(1048576);
    //let bytes = Bytes::from(b"hello world".as_ref());

    //rbuf.write(common::seq32::Seq32::from(0), &bytes);
    println!("Hello, world!");
}
